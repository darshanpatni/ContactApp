<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:ContactApp="http://schemas.android.com/apk/res-auto">
    <group
        android:id="@+id/search_group"
        ><item android:id="@+id/action_search"
        android:title="@string/app_name"
        android:icon="@drawable/ic_open_search"
        ContactApp:showAsAction="always"

        ContactApp:actionViewClass="android.widget.SearchView"
        />
    </group>
    <group
        android:id="@+id/main_menu_group">

        <item
            android:id="@+id/action_delete"
            android:title="@string/app_name"
            android:icon="@drawable/ic_action_delete"
            ContactApp:showAsAction="always"
            android:visible="false"
            />
        <item android:id="@+id/action_close"
            android:title="@string/app_name"
            android:icon="@drawable/ic_action_close"
            android:titleCondensed="Close"
            android:visible="false"
            ContactApp:showAsAction="always"
            />
    </group>

    <group
        android:id="@+id/sortAgenda">
        <item android:id="@+id/sortSpinner"
            android:icon="@drawable/ic_action_sort"
            android:title="Sort"
            android:visible="false"
            ContactApp:showAsAction="always"
            ContactApp:actionViewClass="android.widget.Spinner" />
    </group>



</menu>