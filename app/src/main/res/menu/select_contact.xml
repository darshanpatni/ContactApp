<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:ContactApp="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_assign_location"
        android:title="@string/app_name"
        android:icon="@drawable/ic_action_assign_location"
        android:titleCondensed="Assign Location"
        ContactApp:showAsAction="always"
        />

    <item android:id="@+id/action_delete"
        android:title="@string/app_name"
        android:icon="@drawable/ic_action_delete"
        ContactApp:showAsAction="ifRoom"
        />

</menu>